-- Migration v1 -> v2: add the optional snooze-override column.
-- Nullable ISO-8601 Instant text; existing rows default to NULL (not snoozed).
ALTER TABLE ReminderEntity ADD COLUMN snoozedUntil TEXT;
